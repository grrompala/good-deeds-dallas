// PostCSS processes your CSS before the browser sees it.
// This config is boilerplate required for Tailwind to work — you rarely need to touch it.
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
