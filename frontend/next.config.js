/** @type {import('next').NextConfig} */
const nextConfig = {
  // 'export' mode generates a fully static site (plain HTML/CSS/JS files)
  // that can be hosted anywhere — GitHub Pages, Netlify, Vercel, even a USB drive.
  // Remove this line if you later want server-side rendering on a Node.js host.
  output: 'export',
}

module.exports = nextConfig
